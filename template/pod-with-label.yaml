apiVersion: 1.0
kind: pod
metadata:
  name: name-pod
  labels:
    label: label_value
spec: 
  container:
    - name: name-container
      image: image-container
      ports:
        - container-port: 80